============
Installation
============

At the command line::

    $ easy_install mochi-kernel

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv mochi-kernel
    $ pip install mochi-kernel
